import React from 'react';
import "./style.scss"

function Main() {
  return (
    <div styleName={txt.join(['body ', " item", text === "lorem" && "loading" ])}>Main</div>
  );
}

export default Main;